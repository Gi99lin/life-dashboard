{
  "name": "dashboard",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:demo": "vite build --outDir dist-demo",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vite": "^8.0.4",
    "vitest": "^4.1.8"
  },
  "dependencies": {
    "chart.js": "^4.5.1",
    "socket.io-client": "^4.8.0"
  }
}
